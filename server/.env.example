# Server Configuration
PORT=5000

# Firebase Configuration
# You can get the Service Account JSON from Firebase Console -> Project Settings -> Service Accounts
# Encode the entire JSON file to Base64 to use it here, or modify firebase.js to use a file path.
FIREBASE_SERVICE_ACCOUNT_BASE64=your_base64_encoded_service_account_json
FIREBASE_DATABASE_URL=https://your-project-id.firebaseio.com

# MongoDB Configuration (Specified in requirements)
MONGODB_URI=mongodb://localhost:27017/mirror_mind
